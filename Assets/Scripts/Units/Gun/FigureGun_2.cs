using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FigureGun_2 : Figure
{

    public override void SetPlatformForUnit(SpawnPlatform platform)
    {
        base.SetPlatformForUnit(platform);
    }
}
